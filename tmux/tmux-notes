# Key bind

prefix+s    --> list sessions
prefix+[    --> enter copy mode

# prompt for tmux-commands

swap source 3 with target 1
 - swap-window -s 3 -t 1
 - swap-window -t 0
 - bind-key T swap-window -t 0

 - split-window

# Custom key binds

The "bind" is just an alias for "bind-key".

You can set key binds with and without prefix.
To create bind without prefix use `-n` option.
To create bind with repeated input use '-r' option. When hit leader+f and g, it will
treat as leader+f, leader+g.

```.tmux.conf
bind -n PageDown next-window
bind PageUp previous-window

bind -r f run-shell "tmux new-window tmux-sessionizer.sh"
```



# send-keys

# https://stackoverflow.com/questions/33426159/starting-a-new-tmux-session-and-detaching-it-all-inside-a-shell-script
```tmux
send-keys 'vagrant up' C-m
```

C-m means hit return

# Resource

https://www.seanh.cc/2020/12/28/binding-keys-in-tmux/
https://www.youtube.com/watch?v=DzNmUNvnB04&t=21s

bind and send-keys
- https://stackoverflow.com/questions/19313807/tmux-send-keys-syntax

